{% extends 'base.html.twig' %}

{% block title %}{{"Edycja klienta"}}{% endblock %}

{% block leftBody %}
    {% include '/shared/leftSidebar.html.twig' %}
{% endblock %}

{% block body %}
    <h1>{{"Edycja klienta"}}</h1>
    
    <div class="row">
        <div class="col-xl-12">
            {{ include('client/_form.html.twig', {'button_label': 'Zapisz'}) }}
        </div>
    </div>
    <div class="row">
        <div class="col-xl-12">
            {{ include('client/_delete_form.html.twig') }}
        </div>
    </div>
    <div class="row">
        <div class="col-xl-12 text-right">
            <a href="{{ path('client_index') }}"
               class="btn btn-lg btn-primary">{{ "Powrót"|trans }}</a>
        </div>
    </div>
    
{% endblock %}
