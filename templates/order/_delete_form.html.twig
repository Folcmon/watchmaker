<form method="post" action="{{ path('service_delete', {'id': order.id}) }}" onsubmit="return confirm('{{'Czy na pewno usunąć'|trans}}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order.id) }}">
    <button class="btn">{{ "Usuń"|trans }}</button>
</form>
