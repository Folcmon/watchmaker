{% extends 'base.html.twig' %}

{% block title %}{{ "Edycja przedmiotu"|trans }}{% endblock %}

{% block body %}
    <h1>{{ "Edycja przedmiotu"|trans }}</h1>

    <div class="row">
        <div class="col-xl-12">
            {{ include('storage/_form.html.twig', {'button_label': 'Zapisz'}) }}
        </div>
    </div>
    <div class="row">
        <div class="col-xl-12">
            {{ include('storage/_delete_form.html.twig') }}
        </div>
    </div>
    <div class="row">
        <div class="col-xl-12 text-right">
            <a href="{{ path('storage_index') }}"
               class="btn btn-lg btn-primary">{{ "Powrót"|trans }}</a>
        </div>
    </div>

{% endblock %}
