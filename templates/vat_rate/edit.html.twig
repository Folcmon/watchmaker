{% extends 'base.html.twig' %}

{% block title %}{{ "Stawki VAT"|trans }} / {{ "Edytuj"| trans }}{% endblock %}

{% block leftBody %}
    {% include '/shared/leftSidebar.html.twig' %}
{% endblock %}

{% block body %}
    <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">
           <a href="{{ path('app_vat_rate_index') }}">{{ "Stawki VAT"| trans }}</a> /</span> {{ "Edytuj" |trans }}</h4>

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">
                    {{ include('vat_rate/_form.html.twig') }}
                </div>
                <div class="card-footer border-top">
                    <div class="col-xl-12">
                        <a href="{{ path('app_vat_rate_index') }}" class="btn btn-warning float-end">
                            {{ "Powrót"|trans }}
                        </a>
                        {{ include('vat_rate/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}