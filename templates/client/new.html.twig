{% extends 'base.html.twig' %}

{% block title %}{{ "Klienci"|trans }} / {{ "Dodaj"| trans }}{% endblock %}

{% block leftBody %}
    {% include '/shared/leftSidebar.html.twig' %}
{% endblock %}

{% block body %}
    <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">
           <a href="{{ path('client_index') }}">{{ "Klienci"| trans }}</a> /</span> {{ "Dodaj" |trans }}</h4>

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">
                    {{ include('client/_form.html.twig') }}
                </div>
                <div class="card-footer">
                    <div class="col-xl-12">
                        <a href="{{ path('client_index') }}"
                           class="btn btn-warning float-end">{{ "Powrót"|trans }}</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
