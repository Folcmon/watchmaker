{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.quantity) }}
    {{ form_row(form.alarmQuantity) }}
    {{ form_row(form.price) }}
    {{ form_row(form.margin) }}
    {{ form_row(form.vatRate) }}
<div class="form-group mb-4">
    <button class="btn btn-primary float-end btn-lg btn-lg">{{ "Zapisz"|trans }}</button>
</div>
{{ form_end(form) }}