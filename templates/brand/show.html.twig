{% extends 'base.html.twig' %}

{% block title %}{{ "Marka"|trans }}{% endblock %}

{% block body %}
    <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">
           <a href="{{ path('app_brand_index') }}">{{ "Marka"| trans }}</a> /</span>
        Nr {{ brand.id }} {{ brand.name }}</h4>
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">
                    <div class="row mb-4">
                        <table class="table table-striped">
                            <tbody>
                            <tr>
                                <th>Id</th>
                                <td>{{ brand.id }}</td>
                            </tr>
                            <tr>
                                <th>{{ "Nazwa"|trans }}</th>
                                <td>{{ brand.name }}</td>
                            </tr>
                            <tr>
                                <th>{{ "Utworzony"|trans }}</th>
                                <td>{{ brand.createdAt ? brand.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                            </tr>
                            <tr>
                                <th>{{ "Zmodyfikowany" }}</th>
                                <td>{{ brand.updatedAt ? brand.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                            </tr>
                            </tbody>
                        </table>
                    </div>
                    <div class="row">
                        <div class="col-md-12">
                            <a href="{{ path('app_brand_edit', {'id': brand.id}) }}"
                               class="btn btn-primary float-end btn-lg">{{ "Edycja"|trans }}</a>
                        </div>
                    </div>
                </div>
                <div class="card-footer border-top pt-4">
                    <div class="col-xl-12">
                        <a href="{{ path('app_brand_index') }}"
                           class="btn btn-warning float-end btn-lg">{{ "Powrót"|trans }}</a>
                        {{ include('brand/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}